"use client"
import Link from "next/link";

export default function Navbar() {
    return (
        <nav className="flex flex-row justify-end items-center px-5 sticky top-0 border-b h-14 bg-background">
                    Language toggle
        </nav>
    );
}